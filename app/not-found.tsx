import type {FC} from 'react';

const NotFound: FC = (): React.ReactElement => (
	<main className="h-screen w-screen flex items-center justify-center">
		<h1>404: Not Found</h1>
	</main>
);

export default NotFound;
